// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Wei-Shaw/sub2api/ent/lotteryactivity"
	"github.com/Wei-Shaw/sub2api/ent/lotterycoupon"
	"github.com/Wei-Shaw/sub2api/ent/predicate"
)

// LotteryCouponQuery is the builder for querying LotteryCoupon entities.
type LotteryCouponQuery struct {
	config
	ctx          *QueryContext
	order        []lotterycoupon.OrderOption
	inters       []Interceptor
	predicates   []predicate.LotteryCoupon
	withActivity *LotteryActivityQuery
	modifiers    []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LotteryCouponQuery builder.
func (_q *LotteryCouponQuery) Where(ps ...predicate.LotteryCoupon) *LotteryCouponQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *LotteryCouponQuery) Limit(limit int) *LotteryCouponQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *LotteryCouponQuery) Offset(offset int) *LotteryCouponQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *LotteryCouponQuery) Unique(unique bool) *LotteryCouponQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *LotteryCouponQuery) Order(o ...lotterycoupon.OrderOption) *LotteryCouponQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryActivity chains the current query on the "activity" edge.
func (_q *LotteryCouponQuery) QueryActivity() *LotteryActivityQuery {
	query := (&LotteryActivityClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lotterycoupon.Table, lotterycoupon.FieldID, selector),
			sqlgraph.To(lotteryactivity.Table, lotteryactivity.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, lotterycoupon.ActivityTable, lotterycoupon.ActivityColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LotteryCoupon entity from the query.
// Returns a *NotFoundError when no LotteryCoupon was found.
func (_q *LotteryCouponQuery) First(ctx context.Context) (*LotteryCoupon, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{lotterycoupon.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *LotteryCouponQuery) FirstX(ctx context.Context) *LotteryCoupon {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LotteryCoupon ID from the query.
// Returns a *NotFoundError when no LotteryCoupon ID was found.
func (_q *LotteryCouponQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{lotterycoupon.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *LotteryCouponQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LotteryCoupon entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LotteryCoupon entity is found.
// Returns a *NotFoundError when no LotteryCoupon entities are found.
func (_q *LotteryCouponQuery) Only(ctx context.Context) (*LotteryCoupon, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{lotterycoupon.Label}
	default:
		return nil, &NotSingularError{lotterycoupon.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *LotteryCouponQuery) OnlyX(ctx context.Context) *LotteryCoupon {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LotteryCoupon ID in the query.
// Returns a *NotSingularError when more than one LotteryCoupon ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *LotteryCouponQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{lotterycoupon.Label}
	default:
		err = &NotSingularError{lotterycoupon.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *LotteryCouponQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LotteryCoupons.
func (_q *LotteryCouponQuery) All(ctx context.Context) ([]*LotteryCoupon, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LotteryCoupon, *LotteryCouponQuery]()
	return withInterceptors[[]*LotteryCoupon](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *LotteryCouponQuery) AllX(ctx context.Context) []*LotteryCoupon {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LotteryCoupon IDs.
func (_q *LotteryCouponQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(lotterycoupon.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *LotteryCouponQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *LotteryCouponQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*LotteryCouponQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *LotteryCouponQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *LotteryCouponQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *LotteryCouponQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LotteryCouponQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *LotteryCouponQuery) Clone() *LotteryCouponQuery {
	if _q == nil {
		return nil
	}
	return &LotteryCouponQuery{
		config:       _q.config,
		ctx:          _q.ctx.Clone(),
		order:        append([]lotterycoupon.OrderOption{}, _q.order...),
		inters:       append([]Interceptor{}, _q.inters...),
		predicates:   append([]predicate.LotteryCoupon{}, _q.predicates...),
		withActivity: _q.withActivity.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithActivity tells the query-builder to eager-load the nodes that are connected to
// the "activity" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LotteryCouponQuery) WithActivity(opts ...func(*LotteryActivityQuery)) *LotteryCouponQuery {
	query := (&LotteryActivityClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withActivity = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ActivityID int64 `json:"activity_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LotteryCoupon.Query().
//		GroupBy(lotterycoupon.FieldActivityID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *LotteryCouponQuery) GroupBy(field string, fields ...string) *LotteryCouponGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LotteryCouponGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = lotterycoupon.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ActivityID int64 `json:"activity_id,omitempty"`
//	}
//
//	client.LotteryCoupon.Query().
//		Select(lotterycoupon.FieldActivityID).
//		Scan(ctx, &v)
func (_q *LotteryCouponQuery) Select(fields ...string) *LotteryCouponSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &LotteryCouponSelect{LotteryCouponQuery: _q}
	sbuild.label = lotterycoupon.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LotteryCouponSelect configured with the given aggregations.
func (_q *LotteryCouponQuery) Aggregate(fns ...AggregateFunc) *LotteryCouponSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *LotteryCouponQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !lotterycoupon.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *LotteryCouponQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LotteryCoupon, error) {
	var (
		nodes       = []*LotteryCoupon{}
		_spec       = _q.querySpec()
		loadedTypes = [1]bool{
			_q.withActivity != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LotteryCoupon).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LotteryCoupon{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withActivity; query != nil {
		if err := _q.loadActivity(ctx, query, nodes, nil,
			func(n *LotteryCoupon, e *LotteryActivity) { n.Edges.Activity = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *LotteryCouponQuery) loadActivity(ctx context.Context, query *LotteryActivityQuery, nodes []*LotteryCoupon, init func(*LotteryCoupon), assign func(*LotteryCoupon, *LotteryActivity)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*LotteryCoupon)
	for i := range nodes {
		fk := nodes[i].ActivityID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(lotteryactivity.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "activity_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *LotteryCouponQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *LotteryCouponQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(lotterycoupon.Table, lotterycoupon.Columns, sqlgraph.NewFieldSpec(lotterycoupon.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, lotterycoupon.FieldID)
		for i := range fields {
			if fields[i] != lotterycoupon.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withActivity != nil {
			_spec.Node.AddColumnOnce(lotterycoupon.FieldActivityID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *LotteryCouponQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(lotterycoupon.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = lotterycoupon.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *LotteryCouponQuery) ForUpdate(opts ...sql.LockOption) *LotteryCouponQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *LotteryCouponQuery) ForShare(opts ...sql.LockOption) *LotteryCouponQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// LotteryCouponGroupBy is the group-by builder for LotteryCoupon entities.
type LotteryCouponGroupBy struct {
	selector
	build *LotteryCouponQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *LotteryCouponGroupBy) Aggregate(fns ...AggregateFunc) *LotteryCouponGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *LotteryCouponGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LotteryCouponQuery, *LotteryCouponGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *LotteryCouponGroupBy) sqlScan(ctx context.Context, root *LotteryCouponQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LotteryCouponSelect is the builder for selecting fields of LotteryCoupon entities.
type LotteryCouponSelect struct {
	*LotteryCouponQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *LotteryCouponSelect) Aggregate(fns ...AggregateFunc) *LotteryCouponSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *LotteryCouponSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LotteryCouponQuery, *LotteryCouponSelect](ctx, _s.LotteryCouponQuery, _s, _s.inters, v)
}

func (_s *LotteryCouponSelect) sqlScan(ctx context.Context, root *LotteryCouponQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
