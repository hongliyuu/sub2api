// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Wei-Shaw/sub2api/ent/referralrelation"
	"github.com/Wei-Shaw/sub2api/ent/user"
)

// ReferralRelation is the model entity for the ReferralRelation schema.
type ReferralRelation struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// 邀请人的 user_id
	InviterID int64 `json:"inviter_id,omitempty"`
	// 被邀请人的 user_id，唯一约束确保一人只能被邀请一次
	InviteeID int64 `json:"invitee_id,omitempty"`
	// 发放给邀请人的余额奖励（创建时快照，不受后续配置变更影响）
	InviterReward float64 `json:"inviter_reward,omitempty"`
	// 发放给被邀请人的余额奖励（创建时快照）
	InviteeReward float64 `json:"invitee_reward,omitempty"`
	// 奖励是否已发放，防止重复发放（幂等标志）
	RewardGranted bool `json:"reward_granted,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ReferralRelationQuery when eager-loading is set.
	Edges        ReferralRelationEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ReferralRelationEdges holds the relations/edges for other nodes in the graph.
type ReferralRelationEdges struct {
	// Inviter holds the value of the inviter edge.
	Inviter *User `json:"inviter,omitempty"`
	// Invitee holds the value of the invitee edge.
	Invitee *User `json:"invitee,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// InviterOrErr returns the Inviter value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ReferralRelationEdges) InviterOrErr() (*User, error) {
	if e.Inviter != nil {
		return e.Inviter, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "inviter"}
}

// InviteeOrErr returns the Invitee value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ReferralRelationEdges) InviteeOrErr() (*User, error) {
	if e.Invitee != nil {
		return e.Invitee, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "invitee"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ReferralRelation) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case referralrelation.FieldRewardGranted:
			values[i] = new(sql.NullBool)
		case referralrelation.FieldInviterReward, referralrelation.FieldInviteeReward:
			values[i] = new(sql.NullFloat64)
		case referralrelation.FieldID, referralrelation.FieldInviterID, referralrelation.FieldInviteeID:
			values[i] = new(sql.NullInt64)
		case referralrelation.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ReferralRelation fields.
func (_m *ReferralRelation) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case referralrelation.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case referralrelation.FieldInviterID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field inviter_id", values[i])
			} else if value.Valid {
				_m.InviterID = value.Int64
			}
		case referralrelation.FieldInviteeID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field invitee_id", values[i])
			} else if value.Valid {
				_m.InviteeID = value.Int64
			}
		case referralrelation.FieldInviterReward:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field inviter_reward", values[i])
			} else if value.Valid {
				_m.InviterReward = value.Float64
			}
		case referralrelation.FieldInviteeReward:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field invitee_reward", values[i])
			} else if value.Valid {
				_m.InviteeReward = value.Float64
			}
		case referralrelation.FieldRewardGranted:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field reward_granted", values[i])
			} else if value.Valid {
				_m.RewardGranted = value.Bool
			}
		case referralrelation.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ReferralRelation.
// This includes values selected through modifiers, order, etc.
func (_m *ReferralRelation) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryInviter queries the "inviter" edge of the ReferralRelation entity.
func (_m *ReferralRelation) QueryInviter() *UserQuery {
	return NewReferralRelationClient(_m.config).QueryInviter(_m)
}

// QueryInvitee queries the "invitee" edge of the ReferralRelation entity.
func (_m *ReferralRelation) QueryInvitee() *UserQuery {
	return NewReferralRelationClient(_m.config).QueryInvitee(_m)
}

// Update returns a builder for updating this ReferralRelation.
// Note that you need to call ReferralRelation.Unwrap() before calling this method if this ReferralRelation
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ReferralRelation) Update() *ReferralRelationUpdateOne {
	return NewReferralRelationClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ReferralRelation entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ReferralRelation) Unwrap() *ReferralRelation {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ReferralRelation is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ReferralRelation) String() string {
	var builder strings.Builder
	builder.WriteString("ReferralRelation(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("inviter_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.InviterID))
	builder.WriteString(", ")
	builder.WriteString("invitee_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.InviteeID))
	builder.WriteString(", ")
	builder.WriteString("inviter_reward=")
	builder.WriteString(fmt.Sprintf("%v", _m.InviterReward))
	builder.WriteString(", ")
	builder.WriteString("invitee_reward=")
	builder.WriteString(fmt.Sprintf("%v", _m.InviteeReward))
	builder.WriteString(", ")
	builder.WriteString("reward_granted=")
	builder.WriteString(fmt.Sprintf("%v", _m.RewardGranted))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ReferralRelations is a parsable slice of ReferralRelation.
type ReferralRelations []*ReferralRelation
